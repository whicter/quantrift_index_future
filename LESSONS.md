# 开发经验与踩坑记录

**时间跨度**：2026年5月 ~ 6月
**系统**：NQ + ES 多周期 Confluence 趋势跟踪，IB 实盘

---

## 一、策略参数优化

### 问题：1H 过度交易
- 优化前：1131 笔交易，最大回撤 -62.9%
- 根因：`use_squeeze_mr=True` 混入了均值回归信号，`ut_key` 太小导致止损过于敏感
- 修复：关掉 `use_squeeze_mr`，`ut_key` 从 0.72 调到 1.0，`min_score` 从 4 升到 5
- 结果：261 笔，回撤降到 -19.6%，胜率 68.6%

### 经验
- 趋势策略和均值回归策略不能混用同一套评分体系——它们对"信号方向"的定义本质不同
- `ut_key` 是最敏感的参数，直接控制止损频率；宁可慢一点也不要太敏感
- 优化目标：先控回撤，再看 Sharpe，不要只看收益率

---

## 二、Staged TP（分批止盈）的重要性

- 对比测试：staged_tp=True vs False，$100k 起始
- 结果：+$146k vs +$4.5k，相差 32 倍
- 原因：市场经常不能走完整段趋势，先锁定一部分利润大幅提升胜率（45%→56%）
- **结论：分批止盈是这套策略的核心，不可关闭**

---

## 三、实盘基础设施

### 问题：pm2 开机自启需要 sudo
- `pm2 startup launchd` 生成的命令需要 sudo，在 SSH 非交互式环境里没法用
- 方案：手动写 `~/Library/LaunchAgents/pm2.congrenhan.plist`，`launchctl load` 不需要 sudo
- 经验：macOS 用户级 LaunchAgent 是最干净的开机自启方案，不依赖 sudo 也不依赖 cron

### 问题：launchd stop plist 用 pkill 杀进程
- 原来用 `pkill -f live_engine.py` 强杀，pm2 会立刻把它拉起来，等于没停
- 修复：改成 `pm2 stop ib-bot`，让 pm2 优雅停止并标记为 stopped

### 问题：restart_engine.sh 脚本过期
- 脚本里还是老的 `pkill + nohup venv/python` 命令，跟 pm2 架构完全冲突
- 教训：引入新的进程管理工具后，所有运维脚本必须同步更新

---

## 四、数据一致性与安全性

### 问题：ContFuture 连续合约 vs 历史 CSV 数据不同
- 回测用 CSV（旧合约价格），实盘用 ContFuture（当前合约），价格有跳空
- 导致：回测末态显示有仓，但当前 Bar 的指标其实不满足入场条件（幻象信号）
- 修复：`do_connect()` 第 4.5 节——空仓时额外校验当前 Bar 指标是否真正满足入场条件

### 问题：重连后状态文件与 IB 实际持仓不一致
- 场景：引擎崩溃期间止损触发，IB 已平仓，但状态文件还记录着持仓
- 重启后：按旧状态计算 delta，可能反向开仓
- 修复：`do_connect()` 重连时对比 IB 净仓和状态文件之和；不一致时自动修正状态文件并发 Telegram 告警
- 修正逻辑：IB=0 全清；IB≠0 让绝对值最大的 TF 吸收差额

### 问题：open orders 没有在重连时检查
- 引擎崩溃时如果有订单在途，重启后不知道订单状态
- 现状：重连时调用 `reqAllOpenOrders()` 打日志，有未平仓单会告警，但不自动处理
- 未来改进：检测到在途订单时应暂停该品种的新信号，等订单结束后再继续

---

## 五、监控与告警

### 教训：没有告警等于裸奔
- 早期没有任何告警，引擎崩了只能靠手动看日志发现
- 告警触发时机：IB 连接成功/断开、持仓不一致、每笔下单、引擎崩溃重启、周度复盘
- 实现：`tg_alert()` 用线程非阻塞发送，捕获所有异常静默失败，不影响主逻辑

### 教训：state file 是唯一的"内部账本"
- 引擎完全依赖 `live_state.json` 判断当前持仓，IB 只是执行层
- 任何手动操作 TWS 之后，必须同步修改状态文件，否则下次触发会下错单
- 教训：要有一个"状态不一致"的快速检测和告警机制（现在已实现）

---

## 六、多周期仓位叠加的设计逻辑

### 金字塔建仓：高周期重仓，低周期轻仓
策略没有硬编码手数上限，而是通过 ATR 风险公式自然产生金字塔结构：

```
手数 = (净值 × risk_pct%) / (ATR × sl_mult × 合约乘数)
```

不同 TF 的 `risk_pct_pyramid` 设置不同，但必须确保 risk_pct 的增幅大于 ATR 的增幅，才能产生正确的「高周期手数更多」分层。

**⚠️ 陷阱（2026-06-20 修正）**：原来设置 1H:1.0% / 4H:1.5% / 1D:4.5%，看似高周期风险更高，但高周期 ATR 更大，导致实际手数是 1H 最多（倒置金字塔）。修正后的参数：

| 周期 | risk_pct | ATR(NQ) | 止损金额/手 | 净值 $100k 手数 |
|------|----------|---------|------------|----------------|
| 1H   | **0.72%** | ~80pt  | ~$240      | **3 手**        |
| 4H   | **2.4%**  | ~200pt | ~$600      | **4 手**        |
| 1D   | **6.0%**  | ~400pt | ~$1,200    | **5 手**        |

三 TF 全顺势时最大净仓 = 3+4+5 = **12 手 MNQ**（金字塔顶点）。

**各净值档位对照：**

| NQ/周期 | $32k（统一1%）| $60k（Pyramid）| $100k（Pyramid）| $200k（Pyramid）|
|---------|-------------|--------------|----------------|----------------|
| 1H | 1手 | 2手 | 3手 | 6手 |
| 4H | 1手 | 2手 | 4手 | 8手 |
| 1D | 1手 | 3手 | 5手 | 10手 |
| **最大净仓** | **3手** | **7手** | **12手** | **24手** |

**⚠️ 陷阱：小账户风险失真（1 手最小值 floor）**

公式结果 < 1 手时强制取 1 手，导致小账户高周期的实际风险% 远超意图：

| 周期 | $32k 意图风险(1%) | 实际止损 | 实际风险% |
|------|----------------|---------|---------|
| NQ 1H | $320 | $240 | 0.75% ✅ |
| NQ 4H | $320 | $600 | **1.875%** ⚠️ |
| NQ 1D | $320 | $1,200 | **3.75%** ❌ |
| ES 4H | $320 | $375 | **1.17%** ⚠️ |
| ES 1D | $320 | $750 | **2.34%** ⚠️ |

最坏情况（5 TF 同时触发止损）：-$3,165 ≈ **-9.9%**。不会爆仓，但要做好心理准备，不要因单笔 NQ 1D 亏损 -$1,200 而 panic 干预。此策略设计目标是 **$100k+ 账户**，$32k 下比例失真属过渡期正常现象。

**好处**：
- 高周期趋势更可靠，重仓顺势；低周期噪声多，轻仓试探
- ATR 自适应：波动大时止损宽，自动减仓；波动收缩时自动加仓
- 不需要硬编码上限，市场环境变化时自动调整

### 多周期净仓：内置的动态对冲机制
三个 TF 完全独立持仓，净仓 = 三 TF signed_contracts 之和：

**示例**：4H 多 +4 手，1H 多 +3 手，1D 多 0 手 → 净仓 +7 手

当 1H 出现强烈空头信号（bearScore ≥ reversal_score），1H 执行翻空：
- 关闭 1H 多头 -3 手 + 开 1H 空头 -2 手 → delta = -5
- 净仓从 +7 → +2

**好处**：
- 低周期反转时自动减少风险敞口，不需要人工干预
- 高低周期顺势叠加时自动加重仓位；方向分歧时自动降仓
- 比固定仓位更能适应行情节奏——趋势明确时满仓，转折信号出现时已提前减仓

**实现确认**：代码中每个 TF 独立调用 `process_tf()`，各自计算 delta 并下单；`allow_reversal_flip=True` 时翻转信号直接一笔完成平多+开空。

---

## 七、架构选型

### 回测重放法 vs 实时信号法
- 选择了回测重放法：每根 Bar 收盘后重新运行完整回测，读取最终持仓
- 优点：实盘逻辑和回测逻辑完全一致，不存在"实盘和回测信号不同"的问题
- 缺点：每次触发要跑完整回测，延迟约 10-30 秒；ContFuture 数据可能和历史 CSV 有价差

### 三周期独立持仓 vs 主周期信号
- 三个 TF 独立计算、独立持仓、仓位叠加
- 优点：高低周期同向时自动加仓，反向时自动对冲降风险
- 风险：三个 TF 同时持仓时总风险敞口放大 3 倍，净值较小时要注意

### pm2 + launchd vs crontab vs Docker
- 当前：pm2（进程守护）+ launchd（开机自启 + 开收市调度）
- Docker 方案已规划但未实施，等系统稳定后再做
- Docker 的主要价值：IBC 自动登录 IB Gateway，彻底消除每周手动操作

---

## 七、代码质量

### 状态文件格式迁移
- 旧格式：`{"1h": ..., "4h": ..., "1d": ...}`（单品种）
- 新格式：`{"NQ": {"1h": ..., ...}, "ES": {...}}`（多品种）
- 做了向后兼容的自动迁移，但要注意：格式变更时所有使用状态文件的代码都要同步修改

### 全局副作用：`_CaptureStrategy` 类变量
- 策略参数通过类变量传递（`_CaptureStrategy.min_score = ...`），是全局共享状态
- 在三个 TF 串行执行时没问题；如果未来改为并行执行，会有竞态条件
- 现状可接受，但要知道这个设计缺陷存在

---

## 八、ES 策略设计（2026-06-20）

### ES 1H 趋势策略不可行

- ES 1H Sharpe = -0.445，经 30 轮优化仍为负，永久移除
- 根因：ES 由 500 只股票构成，行业分散，1H 周期震荡特性强，趋势跟踪无效
- ES 4H 和 1D 趋势策略有效（Sharpe 1.025 / 0.657），应保留

### 均值回归（MR）策略关键经验

- **CI > 55 过滤器与信号互斥**：价格到达极端偏离时（RSI<28 + BB下轨），市场往往是短暂趋势下冲，CI 反而降低（变得定向）。CI 过滤几乎屏蔽了所有有效信号（2年只有4个）。移除 CI 过滤。
- **min_score=2 vs 3 天壤之别**：min_score=2（221笔）Sharpe=-1.475；min_score=3（15笔）Sharpe=0.723。宁可每月只做 1 笔，也要确保 3 个信号全中。
- **空头 MR 在牛市无效**：ES 2024-2026 牛市，空头 MR 胜率 38%（多头 55%）。只做多是正确选择，等牛熊切换再评估空头。
- **VWAP 计算需绑定 DatetimeIndex**：backtesting.py 的 `self.data.Close` 是整数索引数组，转成 pandas Series 时要传 `index=DatetimeIndex`，否则 groupby 日期分组失败，VWAP 全为 NaN。
- **R:R = 2:1 是最优组合**：tp=2.0 ATR + sl=1.0 ATR 是测试中最优（Sharpe 0.723），更大的 TP（3.0）反而因持仓时间过长使 Sharpe 下降。
- **同日连续多单是风险点**：回测中 2024-12-18 和 2025-10-10 各触发 2 笔亏损，实盘需加每日最多 1 单限制。

### 多 TF 联合优化的陷阱

ES 4H+1D 联合优化 40 轮，最优分数与初始相同（0.639），说明参数互相干扰。改为单独优化 1D（锁定 4H 参数），ES 1D Sharpe 从 0.253 提升到 0.657。

经验：多 TF 联合优化时，参数空间互相干扰。先优化高 TF 锁定，再单独优化低 TF。

---

## 九、图形形态与反转信号缺失（2026-06-21 分析）

### 问题：策略只有方向指标，没有反转结构识别

**根因**：UT Bot / SSL / MACD / RSI / ADX 全部是趋势方向性指标，告诉你「往哪走」，
但没有任何一个能识别「趋势即将反转」的结构信号。

**典型案例（2025-04-09）**：
- NQ 1D 在 April 7 暴跌底部 17,100 开了空单
- April 9 凌晨 4H 出现明显插针（探低 16,867 → 收回 16,955，下影线 476 pts）
- 同期 Squeeze Momentum Indicator（SMI）出现底背离（价格新低，动量柱未新低）
- 1H 级别同期出现双底形态（April 7 底 + April 9 底）
- 所有这些信号策略全部无视，持仓到 5 月 5 日被打出 -$6,164

**如果在 April 9 4H 插针时止盈**：NQ 在 16,867-17,100 区间平仓，小盈利退出
而不是 5 月 5 日 20,181 出局亏 $6,164，差距约 $7,000+。

### 关键教训

- **指标钝化（divergence/exhaustion）是反转的前置信号**：价格创新低但动量不创新低，
  说明空方力量在减弱，即使趋势还没反转，持续做空的赔率已经在下降。
- **期货很少出现大规模跳空**（6年数据：>2% gap 仅 0.7% 的交易日），大多数反转是在
  盘中走出来的，4H 级别的 pin bar 有足够的时间让策略反应。
- **多时间级别验证原则**：1H 双底 + 4H 插针 + SMI 背离三者共振 → 强反转信号，
  任何一个单独出现 → 弱信号，需要谨慎对待。

### 实现结果与回测对比（2026-06-21）

**NQ 全历史回测，三种配置对比（$100k，2020-2026）**：

| 周期 | 指标 | 关闭 | 双向（v1）| **仅空头（v2，最终）** |
|------|------|------|----------|----------------------|
| NQ 1D | Sharpe | 0.855 | 0.317 ❌ | **0.764** |
| NQ 1D | PnL | +$117k | +$53k ❌ | **+$105k** |
| NQ 4H | Sharpe | 0.662 | 0.540 ❌ | **0.832** ✅ |
| NQ 4H | PnL | +$27k | +$16k ❌ | **+$25k** |
| NQ 1H | Sharpe | 0.718 | 0.796 | **0.849** ✅ |
| NQ 1H | PnL | +$12k | +$14k | **+$16k** ✅ |
| **组合** | **Sharpe** | **3.845** | 2.140 ❌ | **3.322** |
| **组合** | **MaxDD** | -11.4% | -21.9% ❌ | -15.0% |
| **组合** | **PnL** | **+$156k** | +$82k ❌ | **+$146k** |
| 2025 NQ 1D | PnL | -$882 | -$7,368 ❌ | **-$184** ✅ |

### NQ 三TF叠加组合：新旧策略对比（2024-03 ~ 2026-06 公共区间）

> 三 TF 共享资金池，仅统计三 TF 同时有数据的区间（2024-03-01 起）。
> 合约分配：1D > 4H > 1H（信号周期越长权重越大）。

**$32k 档（各 1 手）**：

| 指标 | 旧策略 | 新策略 | 变化 |
|------|--------|--------|------|
| Sharpe | 1.381 | **1.751** | **+0.370** |
| Profit Factor | 1.297 | **1.418** | +0.122 |
| 最大回撤 | -25.89% | -25.31% | +0.58pp |
| 总 PnL | +$13,892 | **+$15,927** | **+$2,036** |
| 年化 | +17.42% | **+19.71%** | +2.29pp |
| 2024 PnL | +$4,063 | +$5,589 | +$1,527 |
| 2025 PnL | -$1,825 | **-$657** | **+$1,169** |
| 2026 PnL | +$11,655 | +$10,995 | -$659 |

**$100k 档（1D=5手 / 4H=4手 / 1H=3手）**：

| 指标 | 旧策略 | 新策略 | 变化 |
|------|--------|--------|------|
| Sharpe | 2.945 | **3.174** | **+0.228** |
| Profit Factor | 1.836 | **1.940** | +0.104 |
| 最大回撤 | **-13.90%** | -18.10% | -4.20pp |
| 总 PnL | +$97,347 | **+$99,620** | **+$2,272** |
| 年化 | +35.21% | **+35.90%** | +0.69pp |
| 2024 PnL | +$27,929 | +$31,282 | +$3,353 |
| 2025 PnL | +$9,891 | +$11,256 | +$1,365 |
| 2026 PnL | +$59,528 | +$57,082 | -$2,446 |

TF 贡献差异（$100k 档）：

| TF | pattern exit | 旧PnL | 新PnL | 差异 |
|----|-------------|-------|-------|------|
| NQ 1D | 关闭 | +$58,376 | +$58,376 | 0 |
| NQ 4H | 开启 | +$26,511 | +$25,219 | -$1,292 |
| NQ 1H | 开启 | +$12,461 | +$16,025 | **+$3,565** |

### 关键结论

- **双向 pattern exit 适得其反**：在 NQ 单边牛市，顶背离假阳性极多，提前砍多头大赢，PnL 腰斩。
- **仅空头 pattern exit 是正确方向**：多头不干预（让趋势跑），只对空仓用底部信号止盈。4H/1H 甚至超越基准，1D 仅少 $12k（主要是 2022 熊市空单被过早止盈）。
- **2025 效果验证**：NQ 1D 年度亏损从 -$882 优化至 -$184，正是 April 2025 这类空单被及时止盈的效果。
- **现行策略（v2）**：`strategy.py` 中 pattern exit 仅在 `d == -1`（空头）时触发，多头完全不受影响。
- **$100k MaxDD 变宽（-13.9%→-18.1%）**：1H pattern exit 大幅增益（+$3,565）改变了回撤时序；Sharpe 和 PF 均提升，风险调整后收益更好，可接受。

---

## 十、VIX 极端恐慌过滤（2026-06-21）

### 问题背景

2025 年 NQ 波动极大（关税冲击、April VIX 飙到 ~60），但组合策略 2025 年只赚 +$11k（$100k档）。
原因：V 形反转行情对趋势跟踪策略不友好——进场太晚（等信号确认），刚开空就遇到底部反弹。

### VIX > 40 的交易逻辑

VIX > 40 代表极端恐慌，历史上几乎每次都是市场阶段性底部（2020 疫情、2025 关税）：

```
VIX > 40 时：
  ├─ 持有空单 → 立即止盈（底部不追空，V形反转风险极高）
  └─ 有底部 pattern 信号 → 开多（捕捉恐慌后的强力反弹）
```

底部 pattern 信号需 ≥ 2 个共振（同 pattern_exit_score）：SMI 底背离、RSI 底背离、Pin Bar（锤子线）、双底。

### 各 TF 测试结果

| TF | 配置 | 原因 |
|----|------|------|
| NQ 1D | exit + entry ✅ | 反弹幅度大（3,000pt+），5手值得抄底 |
| NQ 4H | 关闭 ❌ | exit only 反而切掉空头收益（无入场配合，错失下跌段）|
| NQ 1H | 关闭 ❌ | 同上，VIX > 40 期间 1H 噪声极多，抄底假信号频繁 |

**关键教训**：VIX exit 单独使用在低周期有害——平掉空单后市场继续跌，没有多头配合就等于白白损失空头收益。exit 和 entry 必须配套。

### 组合回测结果（2024-03 ~ 2026-06，$100k 多TF叠加）

| 指标 | 无VIX | 加VIX（1D only）| 变化 |
|------|-------|----------------|------|
| Sharpe | 3.174 | **4.263** | **+1.089** |
| Profit Factor | 1.940 | **2.528** | +0.589 |
| 最大回撤 | -18.10% | **-12.88%** | **+5.21pp** |
| 总 PnL | +$99,620 | **+$135,748** | **+$36,129** |
| 年化收益率 | 35.90% | **46.32%** | +10.41pp |
| 2025 年 PnL | +$11,256 | **+$47,384** | **+$36,129** |

全部增益来自 NQ 1D 在 2025 年 April VIX 飙升时的操作：平掉亏损空单 + 抄底做多捕捉了 16,800→20,000+ 的反弹。

### 实现

- **`indicators.py`**：`compute_signals()` 末尾加载 `data/VIX_1d_*.csv`，按日期 forward-fill 对齐到任意 TF，输出 `vixLevel` 列
- **`strategy.py`**：新增 `use_vix_filter` / `vix_exit_threshold` / `use_vix_entry` / `vix_entry_threshold` 四个类变量；staged_tp 止损后、pattern exit 前插入 VIX exit 逻辑；entry 区块末尾插入 VIX 抄底逻辑
- **`config.yaml`**：NQ 1D `use_vix_filter: true`，`use_vix_entry: true`；NQ 4H/1H `use_vix_filter: false`

---

## 十一、复利 + 动态仓位（年度再平衡）

### 机制

每年初根据累积净值重新计算各 TF 手数，实现仓位随盈利自动放大：

```
手数 = equity × risk% / (ATR × 1.5 × contract_size)
risk%：1D=6.0%  /  4H=2.4%  /  1H=0.72%
ATR 估算：1D≈400pt  /  4H≈200pt  /  1H≈80pt（NQ MNQ 历史均值）
```

三 TF 同方向时最大净仓 = 三者之和（雪球效应：赚得越多，下一期仓位越大）。

### 结果（2024-03 ~ 2026-06，含 VIX 过滤）

| 起始资金 | 期末净值 | 总收益 | 年化 | Sharpe | MaxDD |
|---------|---------|-------|------|--------|-------|
| $30k | $90,945 | +203% | **+63.8%** | 4.223 | -16.8% |
| $100k | $289,350 | +189% | **+60.4%** | 4.541 | -15.7% |
| $200k | $588,425 | +194% | **+61.6%** | 4.471 | -16.0% |

**$100k 档手数演进**：

| 期间 | 净值（期初） | 1D | 4H | 1H | 最大净仓 | 期间收益 |
|------|------------|----|----|----|---------|----|
| 2024-03~12 | $100,000 | 5 | 4 | 3 | 12 | +27.9% |
| 2025全年 | $127,872 | 6 | 5 | 4 | 15 | +46.2% |
| 2026上半年 | $186,907 | 9 | 7 | 6 | 22 | +54.8% |

### 注意事项

- **$30k 年化略高于 $100k** 是 1手 floor 效应（小账户手数取整后实际风险% 偏高），非真正优势
- **年度再平衡** 是保守估计；实盘中 `reference_equity` 每次全平后即时更新，接近连续复利
- ATR 用固定估算值（400/200/80pt），实际 ATR 每年不同；高波动年（如 2025）ATR 偏大会导致实际手数略少于回测估算

---

## 十二、ES 子系统策略更新（2026-06-21）

### 背景

在 NQ 完成 pattern exit + VIX 过滤后，对 ES 子系统做同等评估。
ES（MES，$5/pt）与 NQ 关键差异：
- ES = 500只股票，行业更分散，趋势性弱于 NQ，但 VIX 直接与 ES 挂钩（VIX 是 SP500 波动率）
- ES 1H 趋势策略已永久移除（Sharpe=-0.445）；本次只测 ES 1D（5手）+ ES 4H（4手）
- ES 4H 基准 Sharpe=1.064，远高于 NQ 4H 0.662，说明 ES 4H 趋势信号质量更好

### 回测结果（$100k，四场景）

**ES 1D（2020-01 ~ 2026-06）**

| 场景 | 笔数 | 胜率 | Sharpe | MaxDD | PnL |
|------|------|------|--------|-------|-----|
| A Baseline（无exit无VIX） | 98 | 83.7% | 0.637 | -9.77% | +$44,551 |
| B 短空exit only | 98 | 79.6% | 0.665 | -7.68% | +$43,735 |
| C 双向exit | 105 | 74.3% | 0.449 | -14.68% | +$29,843 ❌ |
| **D VIX过滤（短空exit+VIX）** | 98 | 79.6% | **0.670** | **-8.21%** | **+$48,320** ✅ |

**ES 4H（2024-06 ~ 2026-06）**

| 场景 | 笔数 | 胜率 | Sharpe | MaxDD | PnL |
|------|------|------|--------|-------|-----|
| **A Baseline（无exit）** | 84 | 88.1% | **1.064** | -9.59% | **+$19,405** ✅ |
| B 短空exit only | 88 | 76.1% | 0.821 | -8.61% | +$13,765 ❌ |
| C 双向exit | 76 | 64.5% | 0.440 | -10.09% | +$6,632 ❌ |

### 关键发现

**ES 4H pattern exit 有害（与 NQ 结论相反）**：
- ES 4H 趋势信号质量极高（基准 Sharpe=1.064），pattern exit 在 2025 年把好空头提前砍掉
- 2025年：A +$5,431 vs B +$685（损失 $4,746）
- 结论：**ES 4H 关闭 pattern exit，保持 Baseline**

**ES 1D VIX 过滤有效（与 NQ 1D 结论相同）**：
- D vs A：Sharpe 0.670 vs 0.637，PnL +$48,320 vs +$44,551
- 2025年：D +$31,164 vs A +$8,478（+$22,686，直接来自 VIX>40 平空+抄底）
- 代价：2022年 D = -$2,044 vs A = +$9,967（pattern exit 提前砍 2022 熊市空头）
- 整体权衡：Sharpe 和 MaxDD 均改善，可接受

**为什么 NQ 4H pattern exit 有益但 ES 4H 有害**：
- NQ 4H 基准 Sharpe=0.662，相对较低，空头容易出现假趋势，pattern exit 帮助及时止盈
- ES 4H 基准 Sharpe=1.064，趋势质量高，pattern exit 反而是在高胜率趋势里增加噪声

### 最终 ES 配置

- **ES 1D**：`use_pattern_exit: true`，`use_pattern_long_exit: false`（仅空头），`use_vix_filter: true`，`use_vix_entry: true`，`vix_threshold: 40.0`
- **ES 4H**：`use_pattern_exit: false`，`use_vix_filter: false`（保持纯 Baseline）

---

## 十三、待完成

| 项目 | 优先级 | 说明 |
|---|---|---|
| Pattern exit 仅空头模式 | ✅ 完成 | strategy.py 仅对空头触发（d==-1），多头不受影响，NQ 全历史验证有效 |
| open orders 处理 | 中 | 重连时发现在途订单应暂停该品种信号 |
| ES MR 实盘引擎 | ✅ 完成 | `es_mr/mr_engine.py`，clientId=21，ATR定仓，次季合约，每日1单限制 |
| 每日最多 1 单限制 | ✅ 完成 | mr_engine.py 已内置 today_trades 计数 |
| Docker 化 | 低 | IBC + Gateway + Bot 统一管理 |
